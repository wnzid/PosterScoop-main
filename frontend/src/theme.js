import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#374C63' },
    secondary: { main: '#D48963' },
    background: { default: '#E2DCD0' },
  },
});

export default theme;
